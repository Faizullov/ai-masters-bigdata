entry_points:
  main:
    parameters:
      train_path: {type: string, default: '/home/users/datasets/criteo/criteo_train1'}
      C: {type: int, default: 10}
    command: "python train.py {train_path} {C}"
